<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Splash Screen For Android 12 -->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@color/white</item>
        <item name="windowSplashScreenBackground">@drawable/ic_launcher_background</item>
        <item name="postSplashScreenTheme">@style/Theme.HiFive</item>
    </style>
</resources>